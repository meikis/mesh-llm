# mesh-llm-gpu-bench

GPU benchmark primitives used by mesh-llm hardware profiling.

This crate owns the low-level benchmark runner and GGML probe bindings that
feed measured hardware facts into model-fit and mesh-llm system profiling.
