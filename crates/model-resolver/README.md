# model-resolver

Model reference resolution helpers for Mesh LLM.

This crate resolves model references into concrete artifacts that can be
downloaded, inspected, fitted, or served by higher-level Mesh LLM components.
