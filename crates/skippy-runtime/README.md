# skippy-runtime

Rust runtime support for mesh-llm Skippy staged GGUF execution.

This crate wraps the patched llama.cpp Skippy ABI with Rust-side package,
device, and runtime helpers used by mesh-llm serving and validation tools.
