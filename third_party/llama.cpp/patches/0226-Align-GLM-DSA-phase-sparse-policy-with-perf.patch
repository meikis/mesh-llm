From 8017c17b304e7a0ab930dd8274d8112896b2a454 Mon Sep 17 00:00:00 2001
From: Mesh-LLM CI <ci@mesh-llm.local>
Date: Sat, 4 Jul 2026 09:55:27 +1000
Subject: [PATCH 226/226] Align GLM DSA phase sparse policy with perf

---
 src/llama-graph.cpp        |  2 +-
 tests/test-backend-ops.cpp | 13 ++++++++++++-
 tests/test-llama-archs.cpp | 17 +++++++++--------
 3 files changed, 22 insertions(+), 10 deletions(-)

diff --git a/src/llama-graph.cpp b/src/llama-graph.cpp
index 21ae8fe65..947dc8741 100644
--- a/src/llama-graph.cpp
+++ b/src/llama-graph.cpp
@@ -90,7 +90,7 @@ static bool skippy_glm_dsa_direct_sparse_prefill_enabled() {
         return (filter.glm_dsa_policy_flags & LLAMA_SKIPPY_GLM_DSA_POLICY_DIRECT_SPARSE_PREFILL) != 0;
     }
 
-    return true;
+    return false;
 }
 
 static bool skippy_glm_dsa_unproven_large_direct_sparse_prefill_enabled() {
diff --git a/tests/test-backend-ops.cpp b/tests/test-backend-ops.cpp
index ec484a46b..1061252d0 100644
--- a/tests/test-backend-ops.cpp
+++ b/tests/test-backend-ops.cpp
@@ -7098,6 +7098,11 @@ struct test_dsa_sparse_attn : public test_case {
         return type_k == GGML_TYPE_F16 || type_v == GGML_TYPE_F16 || type_mask == GGML_TYPE_F16 ? 2e-6 : 1e-7;
     }
 
+    uint64_t op_flops(ggml_tensor * t) override {
+        GGML_UNUSED(t);
+        return 2ULL * (uint64_t) n_head * (uint64_t) n_stream * (uint64_t) n_batch * (uint64_t) n_top_k * (uint64_t) (dk + dv);
+    }
+
     bool run_whole_graph() override {
         return true;
     }
@@ -10427,10 +10432,16 @@ static std::vector<std::unique_ptr<test_case>> make_test_cases_perf() {
     test_cases.emplace_back(new test_mul_mat_id(GGML_TYPE_Q2_K, GGML_TYPE_F32, 256, 8, false, 2048, 1, 6144));
     test_cases.emplace_back(new test_mul_mat_id(GGML_TYPE_Q3_K, GGML_TYPE_F32, 256, 8, false, 6144, 1, 2048));
 
-    // GLM-DSA sparse-attention decode shapes.
+    // GLM-DSA sparse-attention phase shapes.
     test_cases.emplace_back(new test_glm_dsa_selected_row_flash(257, 64));
     test_cases.emplace_back(new test_dsa_sparse_attn(GGML_TYPE_F32, GGML_TYPE_F32, GGML_TYPE_F32, 576, 512, 257, 1, 1, 1, 64, 1));
     test_cases.emplace_back(new test_flash_attn_ext(576, 512, 1, {1, 1}, 257, 1, true, false, 0, 0, GGML_PREC_F32, GGML_TYPE_F16, GGML_TYPE_F16));
+    test_cases.emplace_back(new test_dsa_sparse_attn(GGML_TYPE_F32, GGML_TYPE_F32, GGML_TYPE_F32, 576, 512, 257, 4, 1, 1, 64, 1));
+    test_cases.emplace_back(new test_flash_attn_ext(576, 512, 1, {1, 1}, 257, 4, true, false, 0, 0, GGML_PREC_F32, GGML_TYPE_F16, GGML_TYPE_F16));
+    test_cases.emplace_back(new test_dsa_sparse_attn(GGML_TYPE_F32, GGML_TYPE_F32, GGML_TYPE_F32, 576, 512, 257, 8, 1, 1, 64, 1));
+    test_cases.emplace_back(new test_flash_attn_ext(576, 512, 1, {1, 1}, 257, 8, true, false, 0, 0, GGML_PREC_F32, GGML_TYPE_F16, GGML_TYPE_F16));
+    test_cases.emplace_back(new test_dsa_sparse_attn(GGML_TYPE_F32, GGML_TYPE_F32, GGML_TYPE_F32, 576, 512, 257, 16, 1, 1, 64, 1));
+    test_cases.emplace_back(new test_flash_attn_ext(576, 512, 1, {1, 1}, 257, 16, true, false, 0, 0, GGML_PREC_F32, GGML_TYPE_F16, GGML_TYPE_F16));
 
     // gpt-oss-20b
     for (int bs : {1, 4, 8, 512}) {
diff --git a/tests/test-llama-archs.cpp b/tests/test-llama-archs.cpp
index ba6e4a47f..8cb4abad2 100644
--- a/tests/test-llama-archs.cpp
+++ b/tests/test-llama-archs.cpp
@@ -713,14 +713,14 @@ static bool test_glm_dsa_compact_disabled_decode_graph(
     }
 }
 
-static bool test_glm_dsa_verify_direct_sparse_graph(
+static bool test_glm_dsa_verify_dense_graph(
         const size_t seed, const std::vector<ggml_backend_dev_t> & devs) {
     try {
         scoped_test_env graph_reuse_disabled("LLAMA_GRAPH_REUSE_DISABLE", "1");
         scoped_test_env prefill_cap("SKIPPY_GLM_DSA_DIRECT_SPARSE_PREFILL_MAX_TOKENS", "8");
         const glm_dsa_graph_op_counts counts = run_glm_dsa_verify_graph_after_prefill(seed, devs);
 
-        if (counts.dsa_sparse_attn == 0 || counts.compact_get_rows != 0 || counts.sparse_mask != 0 || counts.sparse_mask_nodes != 0) {
+        if (counts.sparse_mask_nodes == 0 || counts.compact_get_rows != 0 || counts.dsa_sparse_attn != 0) {
             fprintf(stderr,
                     "GLM_DSA verification graph counted total=%d dsa_sparse_attn=%d compact_get_rows=%d sparse_mask=%d sparse_mask_nodes=%d flash_attn=%d\n",
                     counts.total, counts.dsa_sparse_attn, counts.compact_get_rows, counts.sparse_mask, counts.sparse_mask_nodes, counts.flash_attn);
@@ -728,19 +728,19 @@ static bool test_glm_dsa_verify_direct_sparse_graph(
         }
         return true;
     } catch (const std::exception & err) {
-        fprintf(stderr, "GLM_DSA verification graph test failed: %s\n", err.what());
+        fprintf(stderr, "GLM_DSA verification dense graph test failed: %s\n", err.what());
         return false;
     }
 }
 
-static bool test_glm_dsa_short_prefill_direct_sparse_graph(
+static bool test_glm_dsa_short_prefill_dense_graph(
         const size_t seed, const std::vector<ggml_backend_dev_t> & devs) {
     try {
         scoped_test_env graph_reuse_disabled("LLAMA_GRAPH_REUSE_DISABLE", "1");
         scoped_test_env prefill_cap("SKIPPY_GLM_DSA_DIRECT_SPARSE_PREFILL_MAX_TOKENS", "8");
         const glm_dsa_graph_op_counts counts = run_glm_dsa_prefill_graph(seed, devs, 8);
 
-        if (counts.dsa_sparse_attn == 0 || counts.compact_get_rows != 0 || counts.sparse_mask != 0 || counts.sparse_mask_nodes != 0) {
+        if (counts.sparse_mask_nodes == 0 || counts.compact_get_rows != 0 || counts.dsa_sparse_attn != 0) {
             fprintf(stderr,
                     "GLM_DSA short prefill graph counted total=%d dsa_sparse_attn=%d compact_get_rows=%d sparse_mask=%d sparse_mask_nodes=%d flash_attn=%d\n",
                     counts.total, counts.dsa_sparse_attn, counts.compact_get_rows, counts.sparse_mask, counts.sparse_mask_nodes, counts.flash_attn);
@@ -748,7 +748,7 @@ static bool test_glm_dsa_short_prefill_direct_sparse_graph(
         }
         return true;
     } catch (const std::exception & err) {
-        fprintf(stderr, "GLM_DSA short prefill graph test failed: %s\n", err.what());
+        fprintf(stderr, "GLM_DSA short prefill dense graph test failed: %s\n", err.what());
         return false;
     }
 }
@@ -757,6 +757,7 @@ static bool test_glm_dsa_long_prefill_dense_mask_guard_graph(
         const size_t seed, const std::vector<ggml_backend_dev_t> & devs) {
     try {
         scoped_test_env graph_reuse_disabled("LLAMA_GRAPH_REUSE_DISABLE", "1");
+        scoped_test_env direct_sparse_prefill("SKIPPY_GLM_DSA_ENABLE_DIRECT_SPARSE_PREFILL", "1");
         scoped_test_env prefill_cap("SKIPPY_GLM_DSA_DIRECT_SPARSE_PREFILL_MAX_TOKENS", "8");
         scoped_test_env dense_mask_limit("SKIPPY_GLM_DSA_DENSE_SPARSE_MASK_MAX_BYTES", "1");
         scoped_test_env large_prefill_enabled("SKIPPY_GLM_DSA_ENABLE_UNPROVEN_LARGE_DIRECT_SPARSE_PREFILL", "1");
@@ -1050,8 +1051,8 @@ static int test_backends(const llm_arch target_arch, const size_t seed, const gg
                 test_glm_dsa_compact_decode_graph(seed, {}) &&
                 test_glm_dsa_compact_min_kv_decode_graph(seed, {}) &&
                 test_glm_dsa_compact_disabled_decode_graph(seed, {}) &&
-                test_glm_dsa_verify_direct_sparse_graph(seed, {}) &&
-                test_glm_dsa_short_prefill_direct_sparse_graph(seed, {}) &&
+                test_glm_dsa_verify_dense_graph(seed, {}) &&
+                test_glm_dsa_short_prefill_dense_graph(seed, {}) &&
                 test_glm_dsa_long_prefill_dense_mask_guard_graph(seed, {});
         if (!glm_dsa_contract_ok) {
             fprintf(stderr, "GLM_DSA IndexShare metadata contract test failed\n");
-- 
2.53.0

